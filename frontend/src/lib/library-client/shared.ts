/** Relocated to lib/api-client/shared.ts once Scheduled Tasks needed the
 * same fetch helpers -- re-exported here so existing imports from
 * @/lib/library-client/shared keep working unchanged. */
export * from "@/lib/api-client/shared";
