/** Relocated to hooks/shared/asyncResource.ts once Scheduled Tasks needed
 * the same store pattern -- re-exported here so existing imports from
 * @/hooks/library/asyncResource keep working unchanged. */
export * from "@/hooks/shared/asyncResource";
